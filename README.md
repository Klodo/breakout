Breakout
========

A minimal clone of the Breakout game.


Build
-----

```
mkdir build
cd build
cmake .. -GNinja
ninja
```

See `install_dependencies.sh` for required dependencies (basically only SDL2).
